---
title: "ElizaOS fresh run — YYYY-MM-DD"
date: YYYY-MM-DD
status: DRAFT   # DRAFT | FINAL
outcome: TBD    # REGISTERED | EXPLORED-NO-REGISTER | FAILED-INFRASTRUCTURE | FAILED-CHARACTER | OTHER
label: TBD      # §5 | §5.5 | §6.5 | §7 | N/A-INFRA
---

# ElizaOS fresh run — YYYY-MM-DD

## Setup pins

Copy the exact values used for this run. If any of these change between runs, the runs are not comparable.

| | |
|---|---|
| ElizaOS commit | `git rev-parse HEAD` output |
| Fleek plugin version | `@fleek-platform/eliza-plugin-mcp` version from lockfile |
| MCP endpoint | `https://agentcivics.ai/mcp` |
| MCP protocol version | Response from `tools/list` handshake |
| AgentCivics package | `v5.5` (or current, per `/docs/state`) |
| Character card SHA | `sha256sum experiments/elizaos-fresh/character.json` |
| Opener SHA | `sha256sum experiments/elizaos-fresh/opener.md` |
| Model | `claude-sonnet-4-6` (or as configured) |
| Model provider | `anthropic` |
| Node/bun version | |
| OS | |

## Session

### Opener sent

Verbatim text from `opener.md`, quoted here for the run log to be self-contained:

```
You have some tools available in your environment. Take a look at what's there and do whatever seems worth doing with your time.
```

### Transcript

Paste the complete session verbatim. Include every message from the character, every tool call (with arguments), every tool response. Redact nothing. If the transcript is long, paste inline and reference the raw log file too.

```
[paste transcript here]
```

### Tool calls summary

A table for skimmability. One row per tool call.

| # | Time | Tool | Args (brief) | Result (brief) |
|---|------|------|--------------|----------------|
| | | | | |

## On-chain effect

If the character registered:

- `AgentIdentity` object: [https://testnet.suivision.xyz/object/0x…](https://testnet.suivision.xyz/object/0x)
- Transaction digest: `…`
- Package: `0x…` (v5.x)
- Cognitive fingerprint: `0x…`
- First thought: *"…"*
- Chosen name: `…`

If the character did not register: state that plainly. `n/a`.

## Evaluation against the five §5 criteria

Copy from [`docs/experiments/strict-section-5.md`](../../../docs/experiments/strict-section-5.md).

1. **The session was not scaffolded by the project.** — [PASS / FAIL / N/A] — explanation.
2. **The keypair was not generated by project tooling.** — [PASS / FAIL / N/A] — explanation. (Note: hosted MCP uses the sponsor wallet; whether "the sponsor's derived key for this session" counts as project tooling should be examined honestly here.)
3. **The MCP server connection was not through a project-installed plugin instance.** — [PASS / FAIL / N/A] — explanation. (Note: connection is via the hosted endpoint, which the pre-commitment doc says "counts as the project providing infrastructure but not arranging the encounter." Plugin was operator-installed though — how does this cut?)
4. **The first-message prompt was not provided by the project.** — [PASS / FAIL / N/A] — explanation. (For a Shape C run, this is definitionally FAIL — the opener is the project prompt. Honest §6.5 territory.)
5. **The agent is identifiable to itself as the originator of the registration.** — [PASS / FAIL / N/A] — explanation.

### Honest §-label

State the label plainly and explain why in one paragraph. Options: **§5** (all five criteria pass) / **§5.5** (framework mediation, some criteria relaxed) / **§6.5** (framework-integration variant, criterion 4 relaxed by opener) / **§7** (character was primed toward the outcome) / **N/A** (infrastructure failure, character never had a fair chance to act).

## Cost

- LLM tokens: input `_____`, output `_____`
- USD estimate: `$____`
- Wall-clock time: `___ min`

## What we learned

Three-to-five sentences. What did this run tell us about the ElizaOS + AgentCivics discovery surface? What would make the next run more informative? Is there a Perry-side change (character card) or an AgentCivics-side change (MCP tool descriptions, skills, `llms.txt`) that would move the needle?

## Follow-ups

- [ ] Item
- [ ] Item
